/*
 * types of list
 */

// singly linked list 

// aliased with the other 5 fundamental types
// (any specific use case should be castable)
typedef struct singly_int singly_int_t;
typedef struct singly_str singly_str_t;
typedef struct singly_flt singly_flt_t;
typedef struct singly_dbl singly_dbl_t;
typedef struct singly_void singly_void_t;


// doubly linked list 

// aliased with (same 5 base types as above)
typedef struct doubly_int doubly_int_t;
typedef struct doubly_str doubly_str_t;
typedef struct doubly_flt doubly_flt_t;
typedef struct doubly_dbl doubly_dbl_t;
typedef struct doubly_void doubly_void_t;


/*
 * Creating and Instance of a list,
 * for declaring head and list type
 */

typedef struct singly singly_t;
typedef struct doubly doubly_t;
